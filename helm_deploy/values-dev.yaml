---
# Per environment values which override defaults in hmpps-prisoner-property-ui/values.yaml

generic-service:
  replicaCount: 2

  ingress:
    host: prisoner-property-dev.hmpps.service.justice.gov.uk

  env:
    INGRESS_URL: "https://prisoner-property-dev.hmpps.service.justice.gov.uk"
    HMPPS_AUTH_URL: "https://sign-in-dev.hmpps.service.justice.gov.uk/auth"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api-dev.prison.service.justice.gov.uk"
    PRISONER_PROPERTY_API_URL: 'https://prisoner-property-api-dev.hmpps.service.justice.gov.uk'
    COMPONENT_API_URL: 'https://frontend-components-dev.hmpps.service.justice.gov.uk'
    DPS_URL: 'https://digital-dev.prison.service.justice.gov.uk'
    ENVIRONMENT_NAME: DEV
    AUDIT_ENABLED: "false"

generic-prometheus-alerts:
  alertSeverity: move-a-prisoner-alerts-nonprod
